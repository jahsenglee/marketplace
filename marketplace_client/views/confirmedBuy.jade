extends ../node_modules/jade-bootstrap/_bootstrap
extends layout.jade
append styles
    link(rel='stylesheet', href='/stylesheets/style.css')

block body
    +navbar-fixed("Marketplace", "dropdown_menu")
        +nav_item("/") Home
        +nav_item("/browse","active") Buy
        +nav_item("/upload") Sell
        form.navbar-form.navbar-left
            form.form-group(action="/search",method="get")
                +input-simple("text","mysearch", "Search","")
                button.btn("submit") Search
        form.navbar-form.navbar-right
            div(id="divId")
                +a-btn-success("/login") Login
            div(id="divIdlogout" style="display:none;")
                +a-btn-success(onclick="toggleLogin('','')" href="/login") Logout
        ul.nav.navbar-nav.navbar-right
            +nav_item("/account") Account
            +nav_item("/watchlist") Watchlist
    block content
        .headerpad
            .container
                h1 Successful Purchase!
                .container
                    .jumbotron
                        .row
                            .col-md-4.col-sm-4.col-xs-12.watchImagePad
                                img.imageProfile.imageRounded(src='images/#{item.images[0]}', alt='ITEM DP', width='250', height='200')
                            .col-md-8.col-sm-8.col-xs-12
                                p $#{item.price} has been deducted from this account
                                p Congratulations on your #{item.name}!



            .footer