extends ../node_modules/jade-bootstrap/_bootstrap
extends layout.jade
append styles
	link(rel='stylesheet', href='/stylesheets/style.css')

block content
	.inside
		h1 Browsing page
		ul
			each item in stock
				li
					p #{item.name}
					p #{item.description}
